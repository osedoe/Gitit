export const loginReducer = (state, action) => {
    switch (action.type) {
        case 1:
            return 1;
        case 2:
            return 1;
        default:
            return state;
    }
};
